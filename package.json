{
  "name": "iteria-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Rokid glasses + desktop guide for the plate-press motion (close -> 10s -> open), driven by vision LLMs.",
  "scripts": {
    "start": "node server/src/main.ts",
    "dev": "node --watch server/src/main.ts",
    "bench": "node tools/bench.ts",
    "train:local": "node tools/train_local.ts"
  },
  "engines": {
    "node": ">=22.6"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.3.0",
    "jpeg-js": "^0.4.4",
    "sharp": "^0.35.4",
    "ws": "^8.18.0"
  }
}
